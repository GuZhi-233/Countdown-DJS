**<span style="font-size:40px;"><span style="color:#ffff00;"> 倒计时·DJS 多功能桌面悬浮倒计时工具</span></span>**

<span style="color:#ff9900;"><span style="font-size:12px;">**当前版本：3.0.9 beta**  
**配置版本：v9**  
**插件系统版本：v0.11**</span></span>

![软件更新头图.jpg](https://raw.gitcode.com/user-images/assets/10122757/65253268-3131-4b1c-9662-af8989c341d0/软件更新头图.jpg '软件更新头图.jpg')

---

> <span style="background-color:#e60000;"><span style="font-size:24px;">***须知***</span></span>
> 本项目由个人开发维护，若遇到 Bug，请反馈至邮箱：2835531424@qq.com 或 guzhiawa@qq.com。  
> 下一个大版本正在开发中，敬请期待。

---



## 一、软件简介

**倒计时·DJS** 是一款基于 **PyQt6** 的桌面增强工具，支持创建多个独立的倒计时悬浮窗。它不仅能显示剩余天数/小时/分钟，更内置了工作日计算、GPU 着色器动态背景、登机牌倒计时、结束提示音、天气、番茄钟、诗词轮播、可视化布局编辑器、插件系统和调试控制台等丰富功能，让您的桌面既美观又高效。

> **开源协议**：MIT License  
> **适用平台**：Windows  
> **界面语言**：中文 / English

---

## 二、基础功能

### 1. 多项目管理

- 同时创建多个悬浮窗，每个窗口独立设置目标日期和精确时间。
- 支持自然日与工作日双模式显示，自动识别中国法定节假日和调休。
- 支持**普通倒计时**与**循环倒计时**两种项目类型：循环项目可设定每周特定星期几重复，自动计算下一个触发时刻，非常适合每周例会、每日打卡等周期性任务。

![微信图片_20260924223600_90_49.png](https://raw.gitcode.com/user-images/assets/10122757/797a6b11-c2a8-45da-b9e9-06a9a6226a38/微信图片_20260924223600_90_49.png '微信图片_20260924223600_90_49.png')

### 2. 外观自由定制

- **背景类型**：纯色、图片（含 GIF 动画）、渐变色、动态壁纸（星空 / 鼠标粒子 / 极光 / 天空 / 潮汐）、壁纸轮播。
- **窗口样式**：透明度、圆角、字体、颜色、阴影、描边、**窗口材质**（玻璃 / 磨砂 / 实底）均可调节。
- **字体与颜色**：每个元素独立设置字体、字号、文字颜色、背景颜色、描边颜色、阴影颜色。
- **所见即所得布局编辑器**：拖拽移动元素位置，支持网格吸附和参考线对齐；选中元素后可通过控制点缩放，字体自动适配；左侧元素列表支持拖拽调整层级顺序；键盘方向键（配合 Shift 加速）可微调元素尺寸。

![微信图片_20260924223655_91_49.png](https://raw.gitcode.com/user-images/assets/10122757/f800f866-c301-463e-88d3-11f41c08286e/微信图片_20260924223655_91_49.png '微信图片_20260924223655_91_49.png')

### 3. 实用桌面挂件

- **天气**：实时温度 + 未来 7 天预报，支持和风天气（QWeather）内置免费额度或自定义 API。新增环形天气组件与天气调试对话框。
- **番茄钟**：单击开始/暂停，双击重置，专注和休息模式自动循环。新增完整三阶段（专注 / 短休 / 长休）与循环节拍。
- **诗词格言轮播**：内置三种难度级别（初级/中级/高级），以打字机效果自动切换，每个项目可独立配置诗语级别。
- **独立浮动控件**：天气、番茄钟、诗词可脱离主窗口独立存在，自由拖拽、缩放、置顶、鼠标穿透。

### 4. 全屏沉浸模式

- 一键全屏，动态壁纸可调节帧率（30/60/120 FPS）和画质（最高 / 高 / 中 / 低）。
- 鼠标移动产生视差效果，右下角弹出悬浮控制面板，方便快捷调整。
- 全屏布局按背景类型独立记忆，切换背景时各自恢复上次布局。
- **全屏内直接编辑**：点击选择元素、拖动移动、拖角缩放、调整透明度、切换显隐。
- 全屏模式下可使用翻页时钟样式的两位数字翻转卡片显示倒计时。
- 天空全屏模式使用登机牌组件展示项目信息与倒计时。
![微信图片_20260924223753_92_49.png](https://raw.gitcode.com/user-images/assets/10122757/fec98086-e517-404a-8abf-604f7bc85144/微信图片_20260924223753_92_49.png '微信图片_20260924223753_92_49.png')
![微信图片_20260924224100_93_49.png](https://raw.gitcode.com/user-images/assets/10122757/2c32f1cc-e24d-4420-8467-5e9692cb1d67/微信图片_20260924224100_93_49.png '微信图片_20260924224100_93_49.png')
### 5. 鼠标穿透模式

- 开启后悬浮窗不再接收任何鼠标事件，所有点击直接穿透到下层窗口，适合将倒计时作为纯桌面装饰。
- 该状态随项目保存，可单独对每个窗口设置。

### 6. 壁纸选择器

- 管理一个图片列表（添加、删除、排序），支持轮播模式（顺序、随机、循环）、切换间隔和填充方式（拉伸、适应、居中、平铺）。
- 支持淡入淡出切换效果，可将轮播壁纸直接应用到悬浮窗背景或系统桌面壁纸（跨平台支持 Windows/macOS/Linux）。
- 支持双屏扩展，可分别为主屏和副屏指定壁纸。

### 7. 插件系统（CAC）

- 安全沙箱 + 权限管理（文件读写、网络、命令），插件需用户授权方可执行敏感操作。
- 提供 API 供开发者注册动态背景、自定义控件和设置面板。
- 内置资源限制机制：单个插件事件处理耗时超过 2 秒累计 3 次后自动禁用，防止失控插件拖垮性能。
- **默认关闭**：插件系统默认不启用，需在设置中手动开启。

### 8. 调试控制台

- 在设置界面按 `~` 或 `` ` `` 键呼出，交互式 Python 环境。
- 支持 Tab 补全、命令历史，内置 20+ 快捷命令（如性能监控、清理缓存、备份配置、打开测试面板等）。
- 可开启日志转发，实时查看系统日志。

### 9. 系统托盘与开机启动

- 托盘菜单支持显示/隐藏所有窗口、重置窗口位置、鼠标穿透、考试模式、更新假期数据、退出应用。
- 内置开机自启动选项（Windows 注册表）。

### 10. 多语言支持

- 在全局设置中可切换中文/English，部分界面即时生效。

### 11. 配置备份与还原

- 设置界面提供“备份配置”和“还原配置”按钮，一键将所有配置（项目、布局、壁纸列表、独立控件等）保存到 `data/backups/` 目录，随时从备份文件恢复。

---

## 三、3.0.9 beta 新功能亮点

### 1. 独立浮动控件系统（最大亮点）

新增完整的独立浮动控件体系，天气、番茄钟、诗词三种控件可脱离主窗口独立存在。

- **FloatWidgetWindow**：独立控件窗口，支持拖拽移动、边缘缩放、滚轮缩放、右键菜单、置顶、鼠标穿透、移除。
- **GlassWeatherWidget**：扁平色块天气挂件，显示天气色标、大字温度、城市与描述，每 30 分钟自动刷新。
- **PomodoroBarWidget**：扁平进度条番茄钟，支持专注/短休/长休三态、阶段色标签、循环节拍。
- **GlassPoemWidget**：扁平卡片诗词控件，楷体竖排诗文、自右向左、出处列居左。
- **WidgetPickerDialog**：控件选择对话框，可预览并添加/移除控件。
- 配置新增 `widget_instances` 字段，持久化独立控件状态。

### 2. 潮汐动态背景

新增 `TideBackgroundWidget` 潮汐背景，根据时间阶段（清晨、白天、黄昏、夜晚）自动切换色调，绘制海浪波纹、颗粒、潮汐光影。支持 `info_mode`，全屏时由背景直接绘制倒计时、天气、潮汐信息。

### 3. 全屏外观选择与全屏内编辑

- **FullscreenAppearanceDialog**：进入全屏前弹出外观选择对话框，包含背景预览卡片、画质选择、帧率选择、GPU 加速提示。
- **FullscreenEditOverlay** 与 **FullscreenEditToolbar**：全屏内直接点击选择、拖动移动、拖角缩放、调整透明度、切换元素可见性。
- 全屏布局新增 `tide` 背景支持，按背景独立存储：default / aurora / sky / tide。

### 4. 窗口材质与主题重做

- 新增 `window_material` 窗口材质：玻璃、磨砂、实底。
- 新增 `window_locked` 窗口锁定，锁定后禁止拖动和缩放，但鼠标交互仍可用。
- 主题色改为暖色琥珀风，新增 `get_spec_tokens` 主题 token 系统。
- 新增数字字体与诗词字体自动解析（Bahnschrift / DIN Condensed / KaiTi / STKaiti 等）。
- 新增 `OpacityShadowEffect` 统一效果系统，支持透明度、阴影、描边、背景统一配置。

### 5. 天气系统全局化

- 新增 `weather_city_mode` 全局天气城市策略：自动定位、手动选择、跟随项目。
- 内置和风天气默认 Key/Host，开箱即用。
- 天气城市选择从元素属性迁移到全局设置。

### 6. 番茄钟完整化

- `PomodoroWidget` 支持专注/短休/长休三阶段、循环次数、调色板、刻度显示、自动下一阶段、阶段提示音。
- 新增 `PomodoroBarWidget` 扁平进度条版本。

### 7. 插件与 GPU 默认安全策略

- 插件系统默认关闭，用户需显式开启。
- GPU 加速默认关闭，极光/天空默认走 CPU 回退，兼容性更好。
- 新增 `_GLCompositingKeeper` 辅助 GL 合成，解决 GL 窗口与普通窗口混合问题。
- 新增 `_StaysOnTopComboBox`，解决下拉框被置顶窗口遮挡问题。

---

## 四、安装与运行

### 环境要求（Python）

- Python 3.8+（推荐 3.10）
- 依赖库：PyQt6, qtawesome, requests, chinese_calendar, Pillow

### Windows

- 操作系统：Windows 7 SP1 及以上版本（建议 Windows 10/11）
- 系统架构：64 位（根据打包选项，也可能支持 32 位）
- 必须的运行时组件：Visual C++ Redistributable for Visual Studio 2015-2022（x64 或 x86，与打包位数一致）。因为 PyQt6 及其依赖的 Qt6 库使用 MSVC 编译，缺少该运行库会导致程序启动失败。多数 Windows 系统可能已安装，若未安装，可从微软官网下载。

### 快速启动

**方式一（推荐）**：下载安装程序，运行即可。  
**方式二（源码）**：

```bash
git clone <repository-url>
pip install PyQt6 qtawesome requests chinese_calendar Pillow
python djs_pyqt6.py
```

### 首次使用

- 首次启动会显示欢迎窗口，帮助快速了解核心功能。
- 右键悬浮窗 → “设置” → “项目管理” → 添加或编辑项目。
- 全局设置中调整渲染后端、日志等级、语言等。
- 插件管理默认关闭，需手动开启并放入 `.py` 插件至 `plugins` 目录。

---

## 五、自定义外观快速上手

1. 在设置 → 项目管理中选中项目，点击 **“自定义外观”**。
2. 左侧为元素列表（可拖拽排序），中间为预览画布（可拖拽元素位置），右侧为属性面板。
3. 选中任意元素可修改字体、颜色、透明度、描边、阴影、背景、对齐方式等；字体大小、透明度、位置等参数配有滑块和数字输入框联动。
4. 取消元素的“自动宽度”后，可分别手动调整宽度和高度；自定义图片元素调整时会自动锁定原始宽高比。
5. 选中元素后可用键盘方向键（配合 Shift 加速）微调尺寸。
6. 编辑未保存时关闭窗口会弹出确认对话框，防止误操作丢失修改。
7. 编辑器右下角提供窗口级快速设置，直接调整透明度、背景类型、动态壁纸、窗口材质等。

> **提示**：软件支持为每个元素单独设置**字体**、**背景颜色**和**字体颜色**，满足个性化需求。

---

## 六、插件系统简介

- **启用**：设置 → 插件管理 → 打开顶部开关。
- **安装**：将 `.py` 文件放入应用根目录下的 `plugins` 文件夹。
- **开发**：继承 `Plugin` 类，实现 `setup(app, api)` 函数，使用 `api` 提供的注册、日志、配置等方法。
- **权限**：在 `requested_permissions` 中声明，启用时由用户确认。
- **API 增强**：`register_dynamic_bg` 新增 `border_radius` 参数；`register_settings_panel`、`register_widget`、`register_settings_widget` 增加重复 ID 检查和类型校验。
- **资源限制**：单个插件事件处理耗时超过 2 秒累计 3 次后自动禁用。

---

## 七、调试与日志

- **日志等级**：全局设置中可选 0（关闭）~ 3（详细调试）。
- **查看日志**：点击“查看日志文件”打开 `data/LOG.txt`，超过 1MB 时自动轮转（保留 `LOG.1.txt`）。
- **调试控制台**：按 `~` 键呼出，支持执行 Python 代码和内置命令（如 `list_windows()`、`set_debug_level(3)`、`test`、`blink_info` 等）。
- **渲染后端**：全局设置可选 auto / software / opengl / vulkan / d3d11 / d3d12 / metal，对显卡驱动有问题的场景很有帮助。

---

## 八、常见问题

- **窗口无法移动**：按住中心区域拖拽；若移出屏幕，通过托盘“重置窗口位置”恢复。
- **天气不显示**：确认已添加天气元素并选择城市；可在全局设置中切换数据源或使用天气调试对话框排查。
- **动态壁纸卡顿**：降低帧率或画质，或改用静态背景；GPU 着色器背景会自动升降档，无需手动干预。
- **GPU 着色器背景显示异常**：确认已在全局设置开启“GPU 着色器加速”，并尝试切换渲染后端；若系统不支持 OpenGL 会自动回退到 CPU 版本。
- **插件加载失败**：检查 `plugins` 目录、插件语法，查看日志文件。
- **全屏模式文字闪烁**：已在新版中通过预先隐藏普通窗口元素修复，若仍出现请反馈。

---

## 九、更新日志

### v3.0.9 beta

#### 新增功能
- **独立浮动控件系统**：天气、番茄钟、诗词可脱离主窗口独立存在，支持拖拽、缩放、置顶、鼠标穿透。
- **潮汐动态背景**：根据时间阶段自动切换色调，绘制海浪、颗粒、潮汐光影，支持全屏信息模式。
- **全屏外观选择与全屏内编辑**：进入全屏前可选择背景、画质、帧率；全屏内可直接编辑元素位置、大小、透明度、显隐。
- **窗口材质**：玻璃 / 磨砂 / 实底三种材质，配合 DWM 毛玻璃效果。
- **窗口锁定**：锁定后禁止拖动和缩放，但鼠标交互仍可用。
- **主题 token 系统**：暖色琥珀主题，数字字体与诗词字体自动解析，统一元素效果系统。
- **天气城市全局策略**：自动定位、手动选择、跟随项目；内置和风默认 Key/Host。
- **番茄钟完整化**：专注/短休/长休三阶段、循环节拍、调色板、刻度、自动下一阶段、提示音。
- **插件系统默认关闭**，GPU 加速默认关闭，提升安全性与兼容性。
- **配置版本升级至 v9**，新增 `widget_instances` 持久化。

#### 改进与优化
- 全屏布局新增 `tide` 背景支持，按背景独立存储。
- 新增 `_GLCompositingKeeper` 与 `_StaysOnTopComboBox`，解决 GL 窗口混合与下拉框遮挡问题。
- 天气地理缓存与中文名回退。
- 独立控件配置持久化。
- 帮助文档更新，新增独立控件、潮汐、窗口锁定等章节。

### v2.9.0 RC1

#### 新增功能
- **GPU 着色器动态背景**：新增天空着色器（体积云 + god rays）与极光着色器（5 层体积光幕 + 星云），基于 OpenGL/GLSL 渲染，支持 CPU 回退。
- **画面画质档位系统**：新增最高 / 高 / 中 / 低四档画质，通过 `uQuality` uniform 驱动着色器参数，运行时自动升降级。
- **登机牌倒计时组件**：新增登机牌样式的倒计时显示，全屏模式下定位在左下角。
- **结束提示音系统**：新增 8 种内置音效 + 自定义音效，项目级配置，多项目同时到期仅播放最高优先级。
- **假期数据系统**：自动注入中国法定节假日与调休，支持后台自动更新。
- **考试模式**：托盘菜单新增考试模式开关。
- **两位数字翻转卡片**：新增 `FlipDigit` / `FlipClockWidget` 组件，全屏翻页时钟显示。
- **测试面板**：新增独立测试面板，集中测试各项功能。
- **环形天气组件**与**天气调试对话框**。
- **调试控制台新增命令**：`test`（打开测试面板）、`blink_info`（查看到期闪烁状态）。
- **调试与工具面板**：设置中心新增分组，集中放置备份配置、显示/重置欢迎按钮。

#### 改进与优化
- **全屏布局系统重构**：按背景类型独立存储布局，新增三套默认布局与全屏布局编辑器。
- **配置系统升级**：config_version v3 → v8，新增 5 段迁移逻辑，保证向后兼容。
- **动态壁纸性能自动降级**：扩展到约 12 处采样点，覆盖多个渲染器，各自独立升降档。
- **天气供应商精简**：统一为和风天气（QWeather），支持内置免费额度或自定义。
- **托盘菜单调整**：去除 emoji 前缀，新增“考试模式”与“更新假期数据”。
- **壁纸选择器性能优化**：引入预加载线程机制，降低切换卡顿。
- **日志系统增强**：动态背景初始化新增详细诊断日志，所有背景类型设置 `_bg_id` 提升可读性。
- **日历按钮健壮性提升**：增加校验与错误日志。
- **鼠标透传属性扩展**：新增 GPU 着色器背景控件统一设置 `WA_TransparentForMouseEvents + NoFocus`。

#### 移除
- 移除旧版 CPU“白云模式”动态背景，由 GPU 天空着色器取代。
- 移除 wttr.in 与 Open-Meteo 天气数据源，统一为和风天气。

### v2.8.3 beta
- 循环项目类型（按星期重复）。
- 壁纸选择器（轮播 / 淡入淡出 / 双屏 / 填充模式）。
- 鼠标穿透模式。
- 配置备份与还原。
- 首次启动欢迎窗口。
- 外观编辑器升级（拖拽排序、键盘微调、脏标记确认）。
- 设置窗口缩放 + 淡入动画、Tab 水平滑动动画。
- 渲染后端选择（auto / software / opengl / vulkan / d3d11 / d3d12 / metal）。
- 数字翻转动画开关。
- 插件系统版本号与错误收集（load_errors / register_errors）。
- 插件资源限制机制。
- 日志轮转功能。

### v2.8.2
- 倒计时显示格式自定义（小数 / 整数 / 时分秒 / 自定义模板）。
- 外观编辑器窗口级快速设置面板。
- 天气 API 可配置（wttr.in / Open-Meteo / 自定义 URL）。
- 多语言支持（中文 / English）。
- 调试控制台大幅升级（日志转发、Tab 补全、方向键历史、20+ 内置命令）。

---

## 十、联系与贡献

- **作者**：苗睿轩
- **贡献**：FIZ_xingchen
- **邮箱**：guzhiawa@qq.com
- 欢迎反馈问题、提交建议或参与贡献。

---

> **祝您使用愉快！**  
> *倒计时·DJS 让每一刻都值得期待。*