```markdown
# 倒计时桌面 · 多功能桌面悬浮倒计时工具
exe可执行文件下载地址:https://1823222148.share.123pan.cn/123pan/tM3gjv-9rwJA


## 一、软件简介

倒计时桌面是一款基于 PyQt6 开发的桌面增强工具，支持创建多个独立的倒计时悬浮窗。除了标准的天/时/分倒计时，还内置了：

- 工作日计算（自动识别中国法定节假日和调休）
- 动态壁纸引擎（星空、白云、鼠标粒子）
- 天气挂件（使用 wttr.in，国内可用）
- 番茄钟（专注/休息循环）
- 诗词格言轮播（三种难度级别）
- 所见即所得的布局编辑器（拖拽排版）
- CAC 插件系统（支持动态背景、自定义控件、设置面板扩展）
- 调试控制台（在设置界面按 `~` 键打开）

> 开源协议：MIT License  
> 适用平台：Windows / macOS / Linux

---

## 二、主要功能列表

- **多项目管理**：同时创建多个悬浮窗，独立设置目标日期/时间，支持自然日与工作日双模式显示。
- **外观自定义**：纯色/图片/渐变/动态壁纸背景，可调节透明度、圆角、字体、颜色、阴影、描边。
- **布局编辑器**：拖拽式排版，可添加文字、图片、天气、番茄钟、诗词等元素，精细调整样式。
- **实用挂件**：天气（实时温度、未来7天预报）、番茄钟（单击开始/暂停，双击重置）、诗词轮播（自动切换）。
- **全屏沉浸模式**：一键全屏，动态壁纸性能可调（帧率/画质），鼠标移动触发视差效果，右下角悬浮控制面板。
- **插件系统**：安全沙箱 + 权限管理（文件读写、网络、命令），提供 API 供开发者扩展。
- **调试控制台**：交互式 Python 环境，可执行代码、查看应用状态、管理插件、调整日志等级等。
- **系统托盘**：隐藏/显示所有窗口、重置窗口位置、快速退出，支持开机自启动。

---

## 三、安装与运行

### 环境要求
- Python 3.8 及以上（推荐 3.10）
- 依赖库：PyQt6, qtawesome, requests, chinese_calendar, Pillow, psutil（可选）

### 快速启动步骤

1. 获取代码(使用Python运行)
2. 安装依赖：pip install PyQt6 qtawesome requests chinese_calendar Pillow

第二个方法:https://1823222148.share.123pan.cn/123pan/tM3gjv-9rwJA在这里下载安装程序

### 首次使用指南

- 右键点击悬浮窗 -> “设置” -> “项目管理” -> “添加项目” 或 “编辑项目”
- 在“全局设置”中调整诗词级别、日志等级、开机自启动等
- 在“插件管理”中开启插件系统（默认关闭），将 `.py` 插件文件放入 `plugins` 目录即可加载

---

## 四、自定义外观教程

### 打开外观编辑器
在设置界面的“项目管理”中选中一个项目，点击 “自定义外观” 按钮。

### 编辑器界面说明
- **左侧**：元素列表（项目名称、倒计时数字、静态文字、天气、番茄钟等）
- **中间**：预览画布，可**直接拖拽元素**调整位置，支持网格吸附和参考线对齐
- **右侧**：属性面板，选中元素后可修改字体、颜色、透明度、描边、阴影、背景圆角、对齐方式等

### 添加/删除元素
- 点击“添加”按钮，可添加内置元素（诗词、天气、番茄钟）或**自定义文字/图片**
- 点击“删除”按钮可移除除“项目名称”和“倒计时”之外的任何元素

### 动态壁纸设置
在项目编辑器中（右键 -> 设置 -> 选中项目 -> 编辑项目）：
- 背景类型 -> 动态
- 选择动态类型：星空、白云、鼠标粒子
- 调整帧率（30/60/120 FPS）和画质（高/中/低）

全屏模式下，鼠标移动到**右下角**会弹出控制面板，可实时切换动态壁纸和调整透明度。

---

## 五、插件系统

### 概述
插件系统采用沙箱 + 权限模型，每个插件运行时受到审计钩子监控，只有被授予的权限才能执行敏感操作（文件、网络、命令）。插件默认全局关闭，需手动启用。

### 安装插件
1. 在应用根目录创建 `plugins` 文件夹（如不存在）。
2. 将 `.py` 插件文件放入其中。
3. 打开“设置” -> “插件管理”，点击顶部开关“启用插件系统”。
4. 插件出现在列表中，选中后点击“启用”，根据提示授予所需权限即可。

### 开发一个简单的插件示例
```python
# my_plugin.py
from plugin_base import Plugin, PluginPermission

class MyFirstPlugin(Plugin):
    def __init__(self, app, api):
        super().__init__()
        self.name = "Hello World"
        self.version = "1.0"
        self.author = "YourName"
        self.description = "示例插件"
        self.requested_permissions = PluginPermission.NONE

    def on_enable(self):
        self.api.log("插件已启用")
        self.api.show_notification("提示", "Hello from plugin!")

def setup(app, api):
    return MyFirstPlugin(app, api)
```

### 插件 API 简要参考
在插件中通过 `self.api` 可调用以下主要方法：

- `api.log(msg, level)`：记录日志。
- `api.register_dynamic_bg(id, name, widget_class)`：注册动态背景。
- `api.register_settings_panel(id, name, widget_class)`：为插件添加设置面板。
- `api.get_config(key, default)` / `api.set_config(key, value)`：读写插件配置。
- `api.show_notification(title, msg)`：发送系统通知。

---

## 六、开发调试与日志

### 调试控制台
在**设置界面**按下键盘上的 `~` 或 `` ` `` 键，弹出交互式 Python 控制台。支持执行任意 Python 代码，并内置了快捷命令，例如：

```
list_windows()          # 列出所有悬浮窗
set_window_alpha(0.8)   # 设置透明度
reload_theme()          # 重新加载主题
plugins()               # 查看已加载插件
enable_plugin("名称")    # 启用插件
set_debug_level(3)      # 开启详细日志
refresh_weather()       # 手动刷新天气
```

### 日志文件
日志保存在 `data/LOG.txt`，可通过“设置 -> 调试与日志 -> 查看日志文件”打开。日志等级可在全局设置中调整：
- 0：关闭
- 1：仅错误/警告
- 2：普通信息（默认）
- 3：详细调试（包含插件调用细节）

---

## 七、常见问题

### 1. 窗口无法移动或调整大小？
- 移动：按住窗口**中心区域**拖拽。
- 调整：鼠标移动到**窗口边缘或角落**，光标变化后拖拽。
- 若窗口移出屏幕，可通过**系统托盘菜单** -> “重置窗口位置”恢复。

### 2. 天气挂件不显示或显示“天气离线”
- 确认已添加天气元素并选择了城市（省份/城市）。
- 国内默认使用 wttr.in，部分地区可能需要代理或更换 API（可修改代码中 WeatherFetcher 的 api_provider）。
- 检查网络连接，或等待下次自动刷新（每 15 分钟）。

### 3. 动态壁纸卡顿怎么办？
- 降低帧率（如从 60 FPS 降到 30 FPS）。
- 降低画质（高 -> 中/低）。
- 改用静态图片或纯色背景。

### 4. 插件加载失败？
- 确保 plugins 文件夹存在，且插件文件以 .py 结尾。
- 检查“插件管理”顶部的“启用插件系统”是否打开。
- 查看 data/LOG.txt 中的详细错误信息（语法错误、缺少依赖等）。

### 5. 如何备份所有配置？
- 整个 data 文件夹包含了配置文件、用户图片、日志、诗词库等。
- 备份该文件夹即可，恢复时替换即可。

---

## 八、版本信息

当前版本：2.8.1 (Build 101)  
Python 版本：3.10+  
操作系统：Windows / macOS / Linux  
数据目录：应用根目录下的 data 文件夹

---

## 九、联系与贡献

本软件由 苗睿轩 开发维护。欢迎反馈问题和建议：guzhiawa@qq.com

如果您觉得本工具对您有帮助，欢迎分享给更多朋友。